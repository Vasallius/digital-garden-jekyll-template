To customize font styles in cells, important, import the `Font()` function from the `openpyxl.styles` module.

![[Pasted image 11.png]]

1. `Font` returns a font object and gets stored at the `italic24Font` variable.


3. Set the cell's font using `.font`